#name: Rust
#
#on:
#  push:
#    branches: [ "main" ]
#  pull_request:
#    branches: [ "main" ]
#
#env:
#  CARGO_TERM_COLOR: always
#
#jobs:
#  build:
#
#    runs-on: ubuntu-latest
#
#    steps:
#    - uses: actions/checkout@v4
#      # 1. ADD THIS STEP HERE TO SET UP YOUR TOOLCHAIN AUTOMATICALLY
#    - name: Setup Rust Toolchain
#      uses: actions-rust-lang/setup-rust-toolchain@v1
#      # No extra inputs needed! It automatically reads your rust-toolchain.toml
#    - name: Build
#      run: cargo build --verbose
#    - name: Run tests
#      run: cargo test --verbose
